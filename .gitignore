# ====================
# PCB 工具临时文件
# ====================
*.lck
*.bak
*-bak.*
*.tmp
*~
.~lock.*

# ====================
# KiCad(若用)
# ====================
*-cache.lib
*.kicad_pcb-bak
fp-info-cache
*.kicad_prl
*.kicad_sym
*.pretty

# ====================
# 立创 EDA / Lceda(若用)
# ====================
*.lceda.bak
*.eco
*.eeschema.bak

# ====================
# Python(若用脚本生成网表 / DRC)
# ====================
__pycache__/
*.pyc
*.pyo
*.pyd

# ====================
# 个人 / 系统
# ====================
.DS_Store
Thumbs.db
*.swp
*.swo

# ====================
# 编辑器 / IDE
# ====================
.vscode/
.idea/

# ====================
# Eclipse 工程元数据 + AI 工作流(2026-09-15 加)
# 跟 STM32CubeIDE 那个 .project / .mxproject 不冲突(那个在子目录里,已被跟踪)
# 这里只 ignore 根目录的 AI/工作流文件
# ====================
/.project
.workflow/

# ====================
# AI 协作文档(2026-09-02 加,KICKOFF_PROMPT 已删 2026-09-12)
# 这些不是项目产物,只本地用
# ====================
CLAUDE.md

# ====================
# 临时数据 / 日志
# ====================
*.log

# ====================
# 机器狗训练数据(2026-09-11 加)
# 正负样本 WAV 文件,本地保留用于迭代训练,不入 git(单文件大 + 量大)
# voice_data/
#   ├── positive/   # "小龙" 正样本(WAV × 100)
#   └── negative/   # 其他名字负样本(WAV × 200)
# ====================
voice_data/

# ====================
# 语音唤醒模型(2026-09-13 加)
# ONNX 模型 ~857KB,本地保留调试用,不入 git(单文件大 + 训练数据已足够还原)
# docs/legacy/xiaolong.onnx
# ====================
docs/legacy/xiaolong.onnx

# ====================
# X3 端 KWS 训练产物(2026-09-15 加)
# voice_data/ 已在顶部 voice_data/ 规则匹配,这里只管 kws/ 目录
# - .venv/        : Python venv(几百 MB,本地复现用,不入库)
# - model_xiaolong/: 训练中间产物(.npy / *_train / *_test 切分 / .pt / .onnx)
# - __pycache__/  : 重复声明保险(已有 __pycache__/ 通配)
# ====================
X3端/kws/.venv/
X3端/kws/model_xiaolong/
X3端/kws/__pycache__/

# ====================
# PC 端 brain 服务(2026-09-16 加)
# - PC端/.venv/    : venv 几百 MB,本地用,不入库
# - PC端/**/__pycache__/ : 字节码缓存
# - PC端/tests/*.wav : 录的测试音频(单文件大 + 个人音色)
# - PC端/tests/*.mp3 : 同上
# ====================
PC端/.venv/
PC端/**/__pycache__/
PC端/tests/*.wav
PC端/tests/*.mp3
PC端/**/*.egg-info/

# 审查临时目录(2026-09-02 加)
_review_tmp/
gerber_review/

# ====================
# STM32 项目编译产物(2026-09-09 加)
# STM32项目/ 是 CubeIDE 工程,只跟踪源码 + 配置,排除 Debug/ 编译产物
# ====================
**/Debug/
**/*.o
**/*.elf
**/*.map
**/*.list
**/*.hex
**/*.bin
**/*.cyclo
**/*.su
**/*.d

# CubeIDE 调试配置(打开工程时会重新生成)
**/*.launch
STM32项目/**/.settings/

# CubeIDE 工程元数据(.cproject / .project / .mxproject 已被跟踪,保留以便工程能打开)
