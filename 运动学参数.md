# 机器狗 · 运动学参数

> 2026-09-10 立。记录几何参数、舵机映射、动作设计推导。
> 状态:v0.1 初稿(蹲下起立阶段)
> 适用范围:机器狗 v1 PCB + 8 路舵机(标定完成后)

---

## 1. 元数据

- 维护者:xiaolongsya
- 最后更新:2026-09-10
- 适用版本:机器狗 v1(2026-09 立项)
- 相关文档:[CLAUDE.md](CLAUDE.md)、[README.md](README.md)

---

## 2. 几何参数

### 2.1 已确认(2026-09-10 用户测量)

| 参数 | 数值 | 单位 | 说明 |
|---|---|---|---|
| R_servo_horn | 50.14 | mm | 小腿舵机偏心半径(舵盘中心 → 连接杆连接点) |
| R_shin_pivot | 20.05 | mm | 小腿连接点偏心半径(膝关节中心 → 连接杆连接点) |
| L_shin | 67 | mm | 小腿长度(膝关节中心 → 脚底) |
| L_thigh | 80 | mm | 大腿长度(髋关节中心 → 膝关节中心) |

### 2.2 TODO:待补测(做严格四连杆模型需要)

| 参数 | 单位 | 说明 |
|---|---|---|
| L_link | mm | 连接杆长度(舵机偏心点 → 小腿偏心点) |
| d_servo_hip | mm | 小腿舵机中心 → 膝关节中心距离 |
| φ_0 | deg | 默认居中(PWM 1500μs)时连接杆方位角(影响远场近似精度) |

---

## 3. 舵机映射

### 3.1 引脚 → servo 编号(来自 CLAUDE.md)

| 引脚 | 定时器通道 | servo 编号 |
|---|---|---|
| PA2 | TIM2_CH3 | servo0 |
| PA3 | TIM2_CH4 | servo1 |
| PA4 | TIM3_CH2 | servo2 |
| PA5 | TIM2_CH1 | servo3 |
| PA6 | TIM3_CH1 | servo4 |
| PA7 | TIM17_CH1 | servo5 |
| PB0 | TIM3_CH3 | servo6 |
| PA8 | TIM1_CH1 | servo7 |

### 3.2 TODO:servo → 腿的映射(用户填)

| servo 编号 | 类型(肩/小腿) | 腿编号 | 备注 |
|---|---|---|---|
| servo0 | ? | ? | TODO |
| servo1 | ? | ? | TODO |
| servo2 | ? | ? | TODO |
| servo3 | ? | ? | TODO |
| servo4 | ? | ? | TODO |
| servo5 | ? | ? | TODO |
| servo6 | ? | ? | TODO |
| servo7 | ? | ? | TODO |

> 4 条腿编号建议:**FL**(前左)/ **FR**(前右)/ **BL**(后左)/ **BR**(后右),等用户确认。

---

## 4. 默认姿态(2026-09-10 标定后)

- 大腿与身体夹角:**90°**(具体几何待确认)
- 大腿与小腿夹角:**90°**(具体几何待确认)
- 所有小腿舵机舵盘朝外
- 所有舵机 PWM 默认 **1500μs**

> TODO:确认默认姿态具体几何(髋关节位置 + 大腿方向 + 小腿方向 + 身体坐标系前向)

---

## 5. 远场近似模型(用户 2026-09-10 提出)

### 5.1 模型假设

四连杆机构(servo horn - 连接杆 - 小腿 - 大腿)在 θ 较小时,连接杆近似"平移",两端竖直方向位移近似相等。

```
Δh_servo_horn = R_servo_horn × f(θ_servo)
Δh_shin_pivot = R_shin_pivot × f(θ_shin)
令两者相等 → 解 θ_shin
```

### 5.2 求解公式

用户采用 `f(x) = 1 - sin(x)`:
```
θ_shin = arcsin(1 - (R_servo_horn / R_shin_pivot) × (1 - sin θ_servo))
```

### 5.3 数值校验(θ_servo = 45°)

```
Δh = 50.14 × (1 - sin 45°) = 50.14 × 0.29289 = 14.687 mm
sin θ_shin = 1 - 14.687/20.05 = 0.26733
θ_shin = arcsin(0.26733) = 15.51°   ← 用户给 14.46°,差约 1°,已校正
```

### 5.4 模型局限性

- **缺少关键参数**:严格四连杆求解需要 L_link、d_servo_hip、φ_0
- **可达区间窄**:θ_servo < 35° 时 Δh > R_shin(20.05mm),物理不可达;可用区间 **40°~60°**
- **几何细节未明确**:`(1 - sin θ)` 公式对应的"端点初始位置 + 旋转方向"待补测确认
- **"身体下降量"公式有歧义**:θ_shin = 0(默认)时按 `67 × (1 - sin θ_shin)` 算下降 67 mm,与"默认不动"矛盾,需实测验证

---

## 6. 蹲下起立动作设计(初稿)

### 6.1 目标

- 调整 4 路小腿舵机,使身体在垂直方向运动
- 4 路肩部舵机保持不动(身体高度 = 脚相对身体高度的函数)
- 4 条腿同步,保持水平

### 6.2 角度-身体下降对照表(基于远场近似)

> 计算:
> - Δh = 50.14 × (1 - sin θ_servo)
> - θ_shin = arcsin(1 - Δh/20.05)
> - 身体下降 = 67 × (1 - sin θ_shin)(公式本身待验证)

| θ_servo (°) | Δh (mm) | θ_shin (°) | 身体下降 (mm) | 备注 |
|---|---|---|---|---|
| < 35 | > 20.05 | 不可达 | — | Δh 超过 R_shin,物理不可能 |
| 40 | 17.91 | 6.13 | 59.85 | 可用区间下界 |
| 45 | 14.69 | 15.51 | 49.09 | 用户验证点 |
| 50 | 11.73 | 24.51 | 39.20 | |
| 55 | 9.07 | 33.21 | 30.31 | |
| 60 | 6.72 | 41.66 | 22.46 | 可用区间上界 |
| 90 | 0.00 | 0.00 | 67.00? | 公式有矛盾,实测后修正 |

> ⚠️ 上表身体下降量的物理意义是"脚相对身体的高度变化"(假设脚不动)。需实测对比。

### 6.3 实测建议

- θ_servo 起始值建议:**45°**(用户已验证的几何点)
- 身体下降范围建议:**20~50 mm**
- 实测后,根据实际俯仰量微调对照表
- 验证方式:用尺子量身体底部到地面的距离,对比目标值

### 6.4 动作序列(待实测)

> TODO:等用户实测后填入

---

## 7. 待办清单(2026-09-10)

### 7.1 待用户回答(影响文档准确性)

- [ ] 8 路舵机的实际分配(哪个 servo = 哪条腿的肩/小腿)
- [ ] 4 条腿的编号约定(FL/FR/BL/BR 是否合适)
- [ ] 默认姿态具体几何(髋关节位置 + 大腿方向 + 小腿方向)
- [ ] 身体前向(头朝向)定义
- [ ] 连接杆初始安装姿态(舵机偏心点初始在哪?最高点/水平点/其他)

### 7.2 待用户补测

- [ ] 连接杆长度 L_link
- [ ] 小腿舵机中心 → 膝关节中心距离 d_servo_hip
- [ ] 默认居中时连接杆方位角 φ_0
- [ ] 实测身体下降量 vs 目标值(填入第 6 节)

### 7.3 待改进

- [ ] 四连杆机构严格模型(知道 L_link、d_servo_hip、φ_0 后)
- [ ] 仿真验证(可借用 py-apple-dynamics 思路)
- [ ] 四腿协调性测试(避免倾斜)

---

## 8. 参考

- [CLAUDE.md](CLAUDE.md) — 舵机分配 + STM32G431 架构
- py-apple-dynamics — 桌面开源运动学工具(参考建模思路)
- 2026-09-10 推理记录:用户在对话中提出几何近似模型,本节整理为文档
