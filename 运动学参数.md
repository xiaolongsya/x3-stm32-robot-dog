# 机器狗 · 运动学参数

> 2026-09-10 立。记录几何参数、舵机映射、动作设计推导。
> 状态:**v0.6**(STM32 身高控制已实现 + Pi 测试脚本 ready)
> 适用范围:机器狗 v1 PCB + 8 路舵机(标定完成后)

---

## 1. 元数据

- 维护者:xiaolongsya
- 最后更新:2026-09-10
- 适用版本:机器狗 v1(2026-09 立项)
- 相关文档:[CLAUDE.md](CLAUDE.md)、[README.md](README.md)

---

## 2. 几何参数

### 2.1 已确认(2026-09-10 用户测量)

| 参数 | 数值 | 单位 | 说明 |
|---|---|---|---|
| R_servo_horn | 50.14 | mm | 小腿舵机偏心半径(舵盘中心 → 连接杆连接点) |
| R_shin_pivot | 20.5 | mm | 小腿连接点偏心半径(膝关节中心 → 连接杆连接点,XY 平面投影) |
| Δz_shin | 5.5 | mm | 小腿连接点相对膝关节中心的高度差(向上) |
| R_shin | 21.22 | mm | 实际距离 √(20.5² + 5.5²) |
| L_link | 60 | mm | 连接杆长度(舵机偏心点 → 小腿偏心点) |
| L_shin | 68 | mm | 小腿有效旋转长度(膝关节中心 → 脚底) |
| L_thigh | 80 | mm | 大腿长度(髋关节中心 → 膝关节中心) |

### 2.2 初始姿态下的连接杆方位(2026-09-10 用户测量)

| 参数 | 数值 | 单位 | 说明 |
|---|---|---|---|
| d_X | 18 | mm | 连接杆两端 X 方向差距(纯前后,无左右) |
| d_Y | ~0 | mm | 连接杆两端 Y 方向差距(几乎为零) |
| d_Z | ~57.24 | mm | 连接杆两端 Z 方向差距(由 L_link 和 d_X 推算:sqrt(60²−18²) ≈ 57.24) |
| 连接杆方向 | (18, 0, 57.24) / 60 ≈ (0.30, 0, 0.95) | 单位向量 | 主要竖直,略带向前(假设舵机偏心点在下) |

> ⚠️ L_link = 60mm < R_servo + R_shin = 70.19mm,远场近似在大角度时误差较大;严格模型见第 5.5 节。

### 2.3 TODO:严格模型还需(几何约束进一步明确后)

> **新发现(2026-09-10)**:
> - **肩部舵机**:转轴 = 大腿方向(±Z),舵盘 = 水平 X-Y,偏心点 P_s(肩) 在水平面内运动
> - **小腿舵机**:转轴 = 垂直小腿方向(±Y),舵盘 = X-Z 平面,**偏心点 P_s(小腿) 在 X-Z 平面内运动**
> - 因 ΔY ≈ 0 + u_s、u_k 都无 Y 分量 → **d_servo_hip.Y 理论为 0,无需测**
> - 旋转统一绕 **Y 轴**(机器狗侧向) → **降为 2D 问题**(X-Z 平面内)

| 参数 | 单位 | 说明 |
|---|---|---|
| **d_servo_hip.X** | mm | 舵机中心相对膝关节中心的**前后**差距(可从连接杆几何反推) |
| **d_servo_hip.Z** | mm | 舵机中心相对膝关节中心的**竖直**差距(可从连接杆几何反推) |
| ~~d_servo_hip.Y~~ | — | **理论为 0,无需测** |
| **α_s** | deg | 舵机偏心点初始方向角(X-Z 平面内,相对 +X 轴) |
| **α_k** | deg | 小腿偏心点初始方向角(X-Z 平面内,相对 +X 轴) |

#### α_s 部分已知(2026-09-10 用户描述)

> 用户描述:"舵盘和传动杆连接点在旋转方向上离舵机中心的 x 距离是 10.5mm"
> 用户描述:"sin A = 10.5 / 转动半径,A 是舵盘和竖直方向的夹角"

- **A = arcsin(10.5 / 50.14) ≈ 12.09°**(偏心方向与竖直方向的夹角)
- **X 分量 = +10.5 mm**(已知)
- **Z 分量 = ±49.02 mm**(符号待用户确认:偏上 / 偏下)
- **α_s ≈ +77.91°**(偏前+上)或 **+102.91°**(偏后+上)或 **−77.91°**(偏前+下)或 **−102.91°**(偏后+下)— **象限待确认**

> 简化提问(目视回答即可):
> - **Q1**: 初始时舵机偏心点 P_s 在舵机中心 O_s 的**上方**还是**下方**?(即 Z 分量是正还是负)
> - **Q2**: **α_k**(小腿偏心点初始方向角)— 目视看初始居中状态,大致象限(前上/前下/后上/后下)
>
> **d_servo_hip 可从几何反推,无需用户测量**:
> - ΔX = d_servo_hip.X + R_shin cos α_k − R_servo cos α_s = 18
> - ΔZ = d_servo_hip.Z + R_shin sin α_k − R_servo sin α_s = 57.24
> - 已知 α_s(符号)、α_k → 反推 d_servo_hip.X 和 d_servo_hip.Z

---

## 3. 舵机映射

### 3.1 引脚 → servo 编号(来自 CLAUDE.md)

| 引脚 | 定时器通道 | servo 编号 |
|---|---|---|
| PA2 | TIM2_CH3 | servo0 |
| PA3 | TIM2_CH4 | servo1 |
| PA4 | TIM3_CH2 | servo2 |
| PA5 | TIM2_CH1 | servo3 |
| PA6 | TIM3_CH1 | servo4 |
| PA7 | TIM17_CH1 | servo5 |
| PB0 | TIM3_CH3 | servo6 |
| PA8 | TIM1_CH1 | servo7 |

### 3.2 servo → 腿的映射(2026-09-10 用户确认)

> **对称原则** — 左右、前后都按对称编号:
> - 前肩部: servo3(FR)、servo4(FL)
> - 前小腿: servo2(FR)、servo5(FL)
> - 后肩部: servo1(BR)、servo6(BL)
> - 后小腿: servo0(BR)、servo7(BL)

| servo 编号 | 类型 | 腿编号 | 引脚 | 定时器通道 |
|---|---|---|---|---|
| servo0 | 小腿 | **BR**(右后) | PA2 | TIM2_CH3 |
| servo1 | 肩部 | **BR**(右后) | PA3 | TIM2_CH4 |
| servo2 | 小腿 | **FR**(右前) | PA4 | TIM3_CH2 |
| servo3 | 肩部 | **FR**(右前) | PA5 | TIM2_CH1 |
| servo4 | 肩部 | **FL**(左前) | PA6 | TIM3_CH1 |
| servo5 | 小腿 | **FL**(左前) | PA7 | TIM17_CH1 |
| servo6 | 肩部 | **BL**(左后) | PB0 | TIM3_CH3 |
| servo7 | 小腿 | **BL**(左后) | PA8 | TIM1_CH1 |

### 3.3 4 条腿编号约定(2026-09-10 用户确认)

- **FL** — 前左(Front Left)
- **FR** — 前右(Front Right)
- **BL** — 后左(Back Left)
- **BR** — 后右(Back Right)

> ✅ 用户认可。身体前向 = servo3(FR 肩)/ servo4(FL 肩)所指方向(纯 X+,见第 4 节)。

---

## 4. 默认姿态(2026-09-10 标定后)

### 4.1 标定居中(PWM 1500)姿态

- 大腿:**垂直地面**(从髋关节向下)
- 小腿:**水平向前**(从膝关节向身体前方延伸)
- 前腿(FL/FR)的脚落在身体**前方**
- 后腿(BL/BR)的脚落在身体**下方**(因髋关节在身体后侧,大腿垂直 + 小腿向前)
- 所有小腿舵机舵盘**朝外**
- 所有舵机 PWM 默认 **1500μs**

### 4.2 坐标系(2026-09-10 用户确认)

| 轴 | 正方向 | 说明 |
|---|---|---|
| **X** | **向前**(头朝向) | servo3(FR 肩)/ servo4(FL 肩)所指方向 |
| **Y** | 向左 | 机器狗左侧 |
| **Z** | **向上** | 重力反方向 |

> 初始连接杆两端的差距:ΔX = 18mm(纯前后),ΔY ≈ 0,ΔZ ≈ 57.24mm(主要竖直)。

### 4.3 期望的"站起来一点"姿态(待实测)

> 用户希望默认姿态比居中(PWM 1500)站得更高一些,意味着要让小腿稍微"收回去"一些(让小腿略向下倾斜,而非完全水平),从而身体被抬起。

- 调整方式:4 路小腿舵机(servo0、servo2、servo5、servo7)**同步微调**
- 目标:身体底部离地面更高(具体增量待实测)
- 验证:量身体底部到地面的距离

> TODO:确定"站起来一点"具体几何 → 目标身体高度 + 4 路小腿舵机的目标 PWM 值

---

## 5. 远场近似模型(用户 2026-09-10 提出)

### 5.1 模型假设

四连杆机构(servo horn - 连接杆 - 小腿 - 大腿)在 θ 较小时,连接杆近似"平移",两端竖直方向位移近似相等。

```
Δh_servo_horn = R_servo_horn × f(θ_servo)
Δh_shin_pivot = R_shin_pivot × f(θ_shin)
令两者相等 → 解 θ_shin
```

### 5.2 求解公式

用户采用 `f(x) = 1 - sin(x)`:
```
θ_shin = arcsin(1 - (R_servo_horn / R_shin_pivot) × (1 - sin θ_servo))
```

### 5.3 数值校验(θ_servo = 45°)

```
Δh = 50.14 × (1 - sin 45°) = 50.14 × 0.29289 = 14.687 mm
sin θ_shin = 1 - 14.687/20.05 = 0.26733
θ_shin = arcsin(0.26733) = 15.51°   ← 用户给 14.46°,差约 1°,已校正
```

### 5.4 模型局限性

- **可达区间窄**:θ_servo < 35° 时 Δh > R_shin(20.05mm),物理不可达;可用区间 **40°~60°**
- **几何细节未明确**:`(1 - sin θ)` 公式对应的"端点初始位置 + 旋转方向"待补测确认
- **"身体下降量"公式有歧义**:θ_shin = 0(默认)时按 `67 × (1 - sin θ_shin)` 算下降 67 mm,与"默认不动"矛盾,需实测验证
- **L_link 较短**:L_link = 60mm < R_servo + R_shin = 70.19mm,远场近似在大角度时误差较大

### 5.5 严格四连杆求解(2D 模型,L_link=60)

> **新发现(2026-09-10)**:小腿舵机转轴方向 = ±Y(垂直小腿),舵盘平面 = X-Z,旋转统一绕 **Y 轴** → 问题降为 **2D 四连杆**,在 X-Z 平面内求解。

#### 5.5.1 几何坐标(单腿 X-Z 平面)

```
 +Z(上)
   |
   ● O_hip (髋关节,肩部舵机输出)
   |
   | L_thigh = 80 mm(垂直地面)
   |
   ● O_knee (膝关节)
   |\
   | \
   |  \  小腿水平向前,L_shin = 67 mm
   |   \
   |    ● O_foot (脚底)
   |
   ● O_s (小腿舵机中心,在 O_knee 上方 + 前方某位置)
   |\
   | \  连接杆 L_link = 60 mm
   |  \
   ●   ● P_k (小腿偏心点,固定在小腿上)
 P_s (舵机偏心点)
   |
   +----→ +X(前)
```

#### 5.5.2 已知量

| 量 | 数值 | 说明 |
|---|---|---|
| L_link | 60 mm | 连接杆长度 |
| R_servo | 50.14 mm | 舵机偏心半径 |
| R_shin | 20.05 mm | 小腿偏心半径 |
| ΔX(P_s → P_k,初始) | 18 mm | 用户测量,纯前后 |
| ΔZ(P_s → P_k,初始) | ≈ 57.24 mm | sqrt(60² − 18²),主要竖直 |
| d_servo_hip.Y | **0(理论)** | ΔY ≈ 0 + u_s、u_k 都无 Y 分量 ⇒ O_s 和 O_k 在同一 Y 平面 |
| α_s 与 +Z 夹角 | **A = 12.09°** | 用户描述 sin A = 10.5/50.14,X 分量 = 10.5 mm |
| α_s 的 Z 分量符号 | **待确认** | 偏心点初始偏上(+)还是偏下(−) |

#### 5.5.4 求解方法(给定 θ_s 求 θ_k)

```
P_s(θ_s) = O_s + R_servo × [cos(α_s + θ_s), sin(α_s + θ_s)]
P_k(θ_k) = O_k + R_shin × [cos(α_k + θ_k), sin(α_k + θ_k)]

约束: |P_s − P_k|² = L_link² = 3600 mm²

展开(P_s、P_k 在 X-Z 平面内):
(d_servo_hip.X + R_shin cos(α_k + θ_k) − R_servo cos(α_s + θ_s))²
+ (d_servo_hip.Z + R_shin sin(α_k + θ_k) − R_servo sin(α_s + θ_s))²
= 3600

Newton 迭代解 θ_k:
1. 初始 θ_k ← 0
2. r = |P_s(θ_s) − P_k(θ_k)|² − 3600
3. ∂r/∂θ_k = 2 × (ΔX × (−R_shin sin(α_k + θ_k)) + ΔZ × (R_shin cos(α_k + θ_k)))
4. θ_k ← θ_k − r / ∂r/∂θ_k
5. 重复直到 |r| < ε(如 1e-6)
```

> 暂时按远场近似(§5.1~5.4)做动作设计。实测后用实测数据反推精确模型;严格 Python 脚本等用户给 2D 参数后写。

---

## 6. 蹲下起立动作设计(初稿)

### 6.1 目标

- 调整 4 路小腿舵机,使身体在垂直方向运动
- 4 路肩部舵机保持不动(身体高度 = 脚相对身体高度的函数)
- 4 条腿同步,保持水平

### 6.2 角度-身体变化对照表(基于远场近似,待用户确认方向)

> 计算(用户 2026-09-10 给的简化公式):
> - Δh_servo = 50.14 × sin θ_servo
> - Δh_shin = 21.22 × sin θ_shin
> - 远场近似 Δh_servo ≈ Δh_shin
> - 身体变化 = ±68 × sin θ_shin(符号取决于方向:提高为 + ,下降为 −)

| θ_servo (°) | Δh_servo (mm) | θ_shin (°) | 身体变化 max (mm) | 备注 |
|---|---|---|---|---|
| 0 | 0.00 | 0.00 | 0.00 | 默认状态 |
| 5 | 4.37 | 11.89 | 14.02 | |
| 10 | 8.71 | 24.18 | 27.85 | |
| 15 | 12.98 | 37.81 | 41.69 | |
| 20 | 17.14 | 53.95 | 54.59 | |
| 25 | 21.19 | 87.61 | 67.94 | Δh 接近 R_shin |
| 30 | 25.07 | 不可达 | — | Δh > R_shin |
| 90 | 50.14 | 不可达 | — | |

> ⚠️ 上表 θ_shin 是按 Δh_servo = Δh_shin 反推,身体变化用 L_shin × sin θ_shin。**严格模型需要数值迭代**(§5.5),且物理方向(提高 vs 下降)取决于舵机旋转方向。

### 6.3 实测建议

- θ_servo 起始值建议:**45°**(用户已验证的几何点)
- 身体下降范围建议:**20~50 mm**
- 实测后,根据实际俯仰量微调对照表
- 验证方式:用尺子量身体底部到地面的距离,对比目标值

### 6.4 动作序列(待实测)

> TODO:等用户实测后填入

---

## 7. 待办清单(2026-09-10)

### 7.1 待用户确认

- [ ] **"站起来一点"目标**:具体目标身体高度(相对当前默认的增量,mm)?
- [ ] 4 条腿编号 FL/FR/BL/BR ✅ 用户认可
- [ ] 身体前向定义(X 正方向 = 头朝向)✅ 用户确认
- [ ] 8 路舵机映射(对称分布)✅ 用户确认

### 7.2 待用户确认(2026-09-10)

> **降维 + 部分消除后只需 2 个参数**:

- [ ] **α_s 的 Z 分量符号**(偏心点初始偏上 / 偏下)— 即 sin A 是 +0.2095 还是 −0.2095
- [ ] **α_k**(小腿偏心点初始方向角,X-Z 平面内)— 目视回答大致象限即可(前上/前下/后上/后下)
- [ ] 实测身体下降量 vs 目标值(填入第 6 节)

> **d_servo_hip 可从几何反推,无需用户测**:
> ΔX = d_servo_hip.X + R_shin cos α_k − R_servo cos α_s = 18
> ΔZ = d_servo_hip.Z + R_shin sin α_k − R_servo sin α_s = 57.24

### 7.3 已完成(2026-09-10)

- [x] 连接杆长度 L_link = 60 mm
- [x] 初始连接杆方位(ΔX=18mm, ΔY≈0, ΔZ≈57.24mm)
- [x] 8 路舵机映射(含引脚 + 腿 + 类型,右后小腿 = servo0,对称分布)
- [x] 默认姿态几何(大腿垂直、小腿水平向前)
- [x] 4 条腿编号(FL/FR/BL/BR)
- [x] 身体前向定义(X+ = 头朝向)
- [x] 数值校正(14.46° → 15.51°,第 5.3 节)

### 7.4 待改进

- [ ] 四连杆机构严格模型(知道 d_servo_hip、u_s、u_k 后,写 Python 迭代求解)
- [ ] 实测对照(目标身体高度 vs 实际)
- [ ] 四腿协调性测试(避免倾斜)
- [ ] 把严格模型移植到 STM32(C 代码)

---

## 8. 参考

- [CLAUDE.md](CLAUDE.md) — 舵机分配 + STM32G431 架构
- py-apple-dynamics — 桌面开源运动学工具(参考建模思路)
- 2026-09-10 推理记录:用户在对话中提出几何近似模型,本节整理为文档

---

## 9. STM32 端身高控制实现(2026-09-10)

### 9.1 简化版身高控制(远场近似)

```c
// 远场近似公式(已在 STM32 main.c 实现)
Δh ≈ (L_shin / R_shin) × ds ≈ 3.2 × ds (mm)
ds ≈ R_servo × sin θ_s ≈ 50.14 × sin θ_s
=> Δh ≈ 3.2 × 50.14 × sin θ_s ≈ 160 × sin θ_s (mm)

1 mm 身体抬升 ≈ sin θ_s ≈ 0.00625 ≈ 0.358° ≈ 4 µs PWM 增量
```

### 9.2 新 UART 命令

| 命令 | 作用 | 示例 |
|---|---|---|
| `<id> <pulse>` | 设单路舵机(0-7) | `5 1561` |
| `all <pulse>` | 设全部 8 路 | `all 1500` |
| `center` | 全部居中 1500 | `center` |
| **`h <delta_mm>`** | **设身体高度增量(新增)** | `h 15` 抬升 15mm,`h -10` 下降 10mm |

### 9.3 实现要点

- 只动 4 路小腿舵机(servo0/2/5/7),肩部不动
- 安全范围:ΔH ∈ [-40, +40] mm,PWM ∈ [500, 2500] µs
- 1 mm 抬升 ≈ 4 µs PWM 增量(常量 `HEIGHT_US_PER_MM`,**待实测校准**)
- 详见 `STM32项目/MachineDog_V1/Core/Src/main.c` 的 `apply_height_delta()` 函数

### 9.4 远场近似 vs 严格求解

- **远场近似**(已实现):1 mm ≈ 4 µs,小角度(< 25°)误差 < 5%
- **严格求解**(未实现):需用 Newton 迭代解四连杆方程,任意角度误差 < 0.1%
- 现阶段用远场近似 + 实测校准;差距大再做严格求解

---

## 10. Pi 端串口配置(2026-09-10)

### 10.1 串口设备

- **/dev/ttyS0** = UART0 = GPIO14(TXD) + GPIO15(RXD)
- 与 STM32 USART1(PA9/PA10)交叉连接:
  - Pi GPIO14(TXD) → STM32 PA10(USART1_RX)
  - Pi GPIO15(RXD) → STM32 PA9(USART1_TX)
  - 共 GND

### 10.2 Pi 配置改动

- `/boot/armbianEnv.txt`:
  - `console=both` → `console=display`(UART0 不再被 console 占用)
  - `overlays=uart5` → `overlays=`(移除有问题的 uart5 overlay)
- `systemctl disable serial-getty@ttyS0.service`(停 agetty)
- 重启后验证:
  - `fuser /dev/ttyS0` 无输出(无进程占用)✓
  - `pyserial` 3.5 已装(系统包 `python3-serial`)✓

### 10.3 Pi 测试脚本

- 路径:`/root/test_stm32_serial.py`
- 功能:测试串口基本通信(center / h 15 / h 0)
- 运行:`python3 /root/test_stm32_serial.py`
- 状态:已测试串口打开 ✓(无 STM32 时发命令无回执,符合预期)

---

## 11. 关键里程碑(2026-09-10)

| 项 | 状态 |
|---|---|
| 几何参数全部已知(L_thigh/L_shin/R_servo/R_shin/L_link) | ✅ |
| 舵机映射(对称分布,8 路) | ✅ |
| 默认姿态(大腿垂直/小腿水平向前) | ✅ |
| STM32 端身高控制代码(简化版) | ✅ 已写入 main.c |
| Pi 端串口配置(/dev/ttyS0 可用) | ✅ |
| Pi 端测试脚本 | ✅ |
| STM32 烧录 + 端到端测试 | ⏳ 待用户执行 |
| 实测校准 `HEIGHT_US_PER_MM` 常量 | ⏳ 待执行 |
| 严格四连杆求解 | ⏳ 后续 |
